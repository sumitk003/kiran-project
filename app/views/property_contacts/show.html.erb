<p id="notice"><%= notice %></p>

<p>
  <strong>Property:</strong>
  <%= @property_contact.property_id %>
</p>

<p>
  <strong>Contact:</strong>
  <%= @property_contact.contact_id %>
</p>

<p>
  <% if @property_contact.classifications.any? %>
    <% @property_contact.classifications.each do |classification| %>
      <span><%= classification.name %></span>
    <% end %>
  <% end %>
</p>

<%= link_to 'Edit', edit_console_account_property_property_contact_path(@account, @property, @property_contact) %> |
<%= link_to 'Back', [:console, @account, @property] %>
