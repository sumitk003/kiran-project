json.extract! contract, :id, :property_id, :for_sale, :for_lease, :sale_price_cents, :sale_price_from_cents, :sale_price_to_cents, :sale_auction_venue, :sale_inspection_on, :sale_reserve_price_cents, :sale_actual_sale_price_cents, :private_treaty_minimum_price_cents, :private_treaty_target_price_cents, :eoi_close_on, :eoi_inspection_on, :eoi_minimum_price_cents, :eoi_target_price_cents, :lease_net_rent, :lease_outgoings, :lease_gross_rent, :lease_total_gross_rent_cents, :lease_cleaning_cents, :lease_covered_parking_space_cents, :lease_on_grade_parking_space_cents, :lease_other_rental_costs_cents, :lease_commencement_on, :lease_expires_on, :lease_term, :lease_rent_review_on, :lease_escalation_rate, :lease_escalation_formulae, :created_at, :updated_at
json.url contract_url(contract, format: :json)
