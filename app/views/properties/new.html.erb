<%= render Page::HeaderComponent.new("New #{@property.type_label} property") %>

<div>

<div class="pb-6">
  <p class="block text-sm font-medium text-gray-700">
    Classification
  </p>
  <div class="mt-2 grid grid-cols-2 gap-3 sm:grid-cols-4">
    <% @property_classifications.each do |classification| %>
      <%= render classification %>
    <% end %>
  </div>
</div>

<%= render 'form', property: @property %>
</div>